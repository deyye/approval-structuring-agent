# v2 验收与优化记录

验证日期：2026-09-09。此公开记录不包含用户原始批文或其派生项目数据。

## 已落实

| 任务 | 成果 |
|---|---|
| 标注与字段口径 | 独立标注结构、字段说明、可传入私有标注路径的评测脚本；公开示例只用虚构数据 |
| 页面解析与14项抽取 | 文本/页码/bbox、固定字段、候选印章、模型引用校验、单文档视图 |
| 动态指标和阶段比较 | 数量单位/上下界/限定词、项目代码归组、阶段确认、多版本列示 |
| 人工复核与导出 | 原文高亮、值与证据修改、修订历史、版本冲突校验、Excel/JSON |
| 测试与部署演示 | 46项自动测试、真实中文OCR测试、虚构两项目六份PDF一键演示、锁定依赖与部署说明 |

## 实测

- 本地完整回归：46项自动化测试通过，包括本地材料回归；私有材料未放入本次提交。
- 公开虚构演示集：84/84固定字段值、84/84来源页码、12/12建设数量指标与独立预期一致。详见`evaluation/latest-report.json`；这些分数不代表真实业务泛化准确率。
- 本地印章候选仍标记需复核，不冒充自动验真。
- 独立中文扫描页经真实OCR读取了预设面积、工期和投资额；不是原生文本层冒充扫描识别。
- 独立Python虚拟环境安装锁定依赖、运行应用及演示通过。
- 浏览器实际走通上传、项目切换、动态指标、单文档、高亮、人工改值和恢复、阶段改动和恢复、重新提取。
- Excel由HTTP接口生成、下载并用openpyxl回读通过；浏览器下载事件两次超时，未将浏览器落地下载认定为通过。
- 前端JavaScript语法检查通过；移动端响应式布局尚未设备端验收。

## 修复

- Decimal数值等价比较，避免不同小数精度被误判。
- 真实日期排序，保留投资口径、约数、上下界、后缀限制词。
- 范围单位重复、跨单位区间、千分位和不兼容维度处理。
- 阶段缺失时比较其余可比值，不填零；避免范围再被截断成单值。
- 标题同一行的章节正文保留，页脚数字按位置与页码识别。
- 阻止负索引修改、无效项目代码、错误证据和陈旧版本覆盖。
- 重新提取时重新读取最新人工确认，防止覆盖并发修订。
- 导出增加单文档和修订记录，并对项目名等进行公式注入转义。
- 比对说明移到整行，减少窄列中的长段落；微小变化不显示误导性的负零百分比。

## 复现

```bash
python scripts/demo.py --data /tmp/approval-demo
python scripts/evaluate.py --samples /tmp/approval-demo/demo-source
python -m unittest discover -s tests -v
python scripts/check_ocr.py
```

真实材料标注可用 `--gold /private/path/gold.json`，评测结果输出到自己的私有目录。公开默认标注只用于虚构演示。

## 未验证的外部条件

- 未提供真实文本/视觉模型服务凭据，尚未完成实调质量测评；可配置接口及失败降级已实现。
- 本环境无Docker守护进程，Dockerfile未实测构建；原生Python部署已实测。
- 任意扫描质量、特殊印章、百万文件吞吐和多人公网部署不属于本次验收覆盖范围。
