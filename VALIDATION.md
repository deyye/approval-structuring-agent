# 验证记录

验证日期：2026-09-09。

## 执行结果

- `SAMPLE_DIR=../upload python -m unittest discover -s tests -v`：23 项测试全部通过。
- `node --check app/static/app.js`：JavaScript 语法检查通过。
- Python 模块编译检查通过。
- HTTP 测试使用临时目录及真实本机 HTTP 请求，验证上传、后台任务、原文 PNG、修订落盘、Excel 导出、文件去重、来源校验和非法 PDF 拒绝。

这23项测试含多个子断言，不代表所有可能版式已验证，也不是“全省通用准确率”。

## 六份批复回归

原始文件来自用户提供的两个项目各三份批复；源码不内置这些 PDF，也不通过文件名返回预填答案。

| 检查项 | 实测结果 |
|---|---|
| 14 项固定字段结构 | 6份均返回完整字段集合，缺失字段使用明确状态 |
| 发文字号 | 六份均与预期值一致，未混入红头/正文请示文号 |
| 原文证据 | 非空字段有证据，文字片段存在于解析行中，坐标处于页面范围内 |
| 庆元总建筑面积 | 约4702 → 3994 → 3576.63平方米 |
| 印发机关 | 龙泉为发展和改革局办公室，庆元未载明，不以正文落款代填 |
| 建设工期 | 庆元建议书/初设缺失，可研14个月；不跨文件补值 |
| 路基宽度 | 24.5米-36米上下界均保留 |
| 印章候选 | 六份均检测到“有”，均要求视觉核对或模型确认 |
| 建设内容跨页 | 页码横线未混入字段内容 |
| Excel | 数值与原文一致、差异颜色存在、证据索引可读取 |

## 仍需在部署环境验收

- 浏览器视觉排版、鼠标点击、不同屏幕尺寸及键盘操作：未进行浏览器端交互测试，仅检查前端语法及后端接口。
- 真实大模型接口：环境无配置凭据，未调用；测试覆盖伪造引用拒绝和合法引用接受。
- 中文扫描件 OCR：当前环境未安装中文语言包，只测试了缺少 OCR 时明确降级；容器声明安装中文 OCR，但 Docker 镜像未在此构建。
- 黑白章、模糊章、特殊版式、其他审批事项及大量文件吞吐尚未验证。
- 本地规则按明确的标题、章节和指标词识别。复杂新增指标需大模型/人工补充，不能把本批样例结果外推为任意文档准确率。

## 源码交付

目标仓库：`deyye/approval-structuring-agent`，主分支 `main`。提交范围为应用代码、测试、运行说明、配置示例和 CI 配置；不包含 `.env`、运行数据及原始批文。
