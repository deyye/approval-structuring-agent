# v2 验收与优化记录

## 本轮任务要求核对（2026-09-11）

现有实现覆盖14项固定字段表格、按项目和阶段比对、页面/Excel差异标色、原文页码及坐标高亮、建设内容数字指标独立成行。印章仍需人工或视觉模型确认；功能覆盖不等于真实材料全部自动提取正确。

本轮修复：
- 同一文本行中不同指标数值相同时，改按数值所在字符位置去重，避免漏掉后一个指标。
- 未预设名称的建设指标使用完整数量表达式，保留“不少于”等限定词及区间。
- 上下限要求、离散计数变化不再被舍入容差判为等同；投资估算/概算口径不同也不会被容差覆盖。
- 自动未检出线索不再在界面表述为“已确认原文未载明”。

验证：新增3项回归测试，覆盖合成PDF抽取、证据页码/坐标、区间及限定词、比较结果与Excel底色。全量156项测试中146项通过，10项因未提供原始6份PDF而跳过。虚构演示集84/84固定字段、84/84证据页码、12/12建设指标通过。JavaScript语法检查通过。本轮未复测真实六份材料、浏览器交互、外部模型或Docker；以下历史记录不代表本轮复测结果。

验证日期：2026-09-09。此公开记录不包含用户原始批文或其派生项目数据。

## 已落实

| 任务 | 成果 |
|---|---|
| 标注与字段口径 | 独立标注结构、字段说明、可传入私有标注路径的评测脚本；公开示例只用虚构数据 |
| 页面解析与14项抽取 | 文本/页码/bbox、固定字段、候选印章、模型引用校验、单文档视图 |
| 动态指标和阶段比较 | 数量单位/上下界/限定词、项目代码归组、阶段确认、多版本列示 |
| 人工复核与导出 | 原文高亮、值与证据修改、修订历史、版本冲突校验、Excel/JSON |
| 测试与部署演示 | 46项自动测试、真实中文OCR测试、虚构两项目六份PDF一键演示、锁定依赖与部署说明 |

## 实测

- 本地完整回归：46项自动化测试通过，包括本地材料回归；私有材料未放入本次提交。
- 公开虚构演示集：84/84固定字段值、84/84来源页码、12/12建设数量指标与独立预期一致。详见`evaluation/latest-report.json`；这些分数不代表真实业务泛化准确率。
- 本地印章候选仍标记需复核，不冒充自动验真。
- 独立中文扫描页经真实OCR读取了预设面积、工期和投资额；不是原生文本层冒充扫描识别。
- 独立Python虚拟环境安装锁定依赖、运行应用及演示通过。
- 浏览器实际走通上传、项目切换、动态指标、单文档、高亮、人工改值和恢复、阶段改动和恢复、重新提取。
- Excel由HTTP接口生成、下载并用openpyxl回读通过；浏览器下载事件两次超时，未将浏览器落地下载认定为通过。
- 前端JavaScript语法检查通过；移动端响应式布局尚未设备端验收。

## 修复

- Decimal数值等价比较，避免不同小数精度被误判。
- 真实日期排序，保留投资口径、约数、上下界、后缀限制词。
- 范围单位重复、跨单位区间、千分位和不兼容维度处理。
- 阶段缺失时比较其余可比值，不填零；避免范围再被截断成单值。
- 标题同一行的章节正文保留，页脚数字按位置与页码识别。
- 阻止负索引修改、无效项目代码、错误证据和陈旧版本覆盖。
- 重新提取时重新读取最新人工确认，防止覆盖并发修订。
- 导出增加单文档和修订记录，并对项目名等进行公式注入转义。
- 比对说明移到整行，减少窄列中的长段落；微小变化不显示误导性的负零百分比。

## 复现

```bash
python scripts/demo.py --data /tmp/approval-demo
python scripts/evaluate.py --samples /tmp/approval-demo/demo-source
python -m unittest discover -s tests -v
python scripts/check_ocr.py
```

真实材料标注可用 `--gold /private/path/gold.json`，评测结果输出到自己的私有目录。公开默认标注只用于虚构演示。

## 未验证的外部条件

- 未提供真实文本/视觉模型服务凭据，尚未完成实调质量测评；可配置接口及失败降级已实现。
- 本环境无Docker守护进程，Dockerfile未实测构建；原生Python部署已实测。
- 任意扫描质量、特殊印章、百万文件吞吐和多人公网部署不属于本次验收覆盖范围。

## 外部模型与 Docker 增量验收（2026-09-10）

新增 13 项模型服务测试，使用真实本地 HTTP 测试服务验证文本/视觉请求、鉴权、限流重试、拒绝重定向、密钥文件、JSON 兼容模式、配置校验和诊断 API。测试仅含合成内容，不使用真实供应商密钥，不代表实际模型质量。

Docker 增加 Compose、非 root 运行、只读根文件系统、健康检查、命名卷和可选 secret 文件。GitHub Actions 新增真实容器构建及重建持久性验收；执行结果以对应提交的 Actions 记录为准。此前 CI 的 OCR 安装被不相关的 Chrome apt 仓库校验失败中断，本次将 apt 更新限制到 Ubuntu 官方源，保留包校验机制。

实际外部服务联调需要在部署环境填入模型地址、模型 ID 和 Key 后运行 check_model.py。当前开发环境无供应商凭据和 Docker 引擎，容器验收由 CI 执行。

## 真实材料驱动的三项修复与自检循环接入（2026-09-11）

用 2 个项目 × 3 个阶段的真实批复（龙泉北互通连接线、庆元县屏都新区全民健身中心）做了一轮
实测，修复了只有真实材料才能暴露的三个问题，并把自检循环接进产品。

### 修正前实测

- 14 项固定字段：79/84 有值，5 个空值回原文核实**均属原文确实未载明**（庆元三份版记无独立
  印发机关行；庆元初设/建议书全文无工期），即 84/84 判断正确。
- 建设指标：44 条，抽查约 30 个数值与原文逐字一致，无编造。
- 自检循环：在这 6 份上**零动作**——主干没有失败，自检也没有信号。

### 修复

| 问题 | 现象 | 处理 |
|---|---|---|
| 同一指标名称不同，跨阶段错位 | 可研「道路路面面积60637平方米」与初设「行车道、路缘带及硬路肩总面积60637平方米」被拆成两行，各自显示「未载明」 | 新增 `align` 状态：数值相同、名称不同、**文档列互不重叠**时提示疑似同一指标，人工确认是否合并 |
| 四舍五入被当成内容变化 | `13.693公顷` 与 `136929.6平方米`（差 0.4 平方米）、`约2.31公里` 与 `2.308千米`（差 2 米）被染成「内容变化」 | 按**原值书写精度**推出容差归入「等同」；单步差异落在容差内时不再给出百分比 |
| 正确的缺失看起来像故障 | 庆元三份「印发机关」连开三个空格子 | 空值分 `absent`（灰，已检索全文核实）与 `unextracted`（红，原文有线索），后者计入待办 |

### 新增能力

- **核对进度**：项目级待办数 + 三档状态（未开始 / 进行中 / 已审完）+ 可点击直达的待办清单；
  每个项目另列「已核实原文未载明」项，不计入待办。
- **自检循环接入**：自检发现异常时才启动（实测这 6 份一次都不启动）；补回的指标与字段一律标
  `needs_review`，来源标为 `agent-widen` / `agent-alt-form` / `agent-read`，文档 `engine` 追加
  `+agent`，`quality` 补救后重算；前端展示每一步的判断过程。
- 导出「单文档结构化」增加**来源**与**缺失原因**两列；界面补齐大模型、视觉模型与自检补救三类来源说明。
- 任务表加上限与清理；上传入参做结构校验，畸形请求返回可读提示而非解释器报错。

### 复现

```bash
python -m unittest discover -s tests          # 133 项通过（与本轮改动前一致）
python scripts/demo.py --data /tmp/approval-demo
python scripts/evaluate.py --samples /tmp/approval-demo/demo-source   # 84/84、84/84、12/12
```

真实材料回归：改动前后逐行对照，除上述 4 行外**逐字不变**；虚构演示集成绩不变。
私有材料与派生结果不进仓库。

## 独立复核与修复（2026-09-11 下午）

对 `581c32c` 做独立复核：不采信提交说明，逐项在真实材料上重跑并回原文核对。

### 复核发现

| 项 | 结果 |
|---|---|
| 全量测试 | 133 项通过，但**新代码零新增测试**——666 行改动没有任何断言覆盖 |
| 三项修复 | 在 6 份真实批复上均生效，且真实变化未被吞掉（挖方 -9.54%、填方 +14.13%、设计速度 80→60、总建筑面积三阶段递减仍报「内容变化」） |
| 指标对齐提示 | 龙泉「道路路面面积」↔「行车道、路缘带及硬路肩总面积」正确标出；同文档内「涵洞7处/平交口7处」未被误标 |
| 空值原因 | 庆元三份「印发机关」标 `absent`，与版记确实无独立署名行一致 |
| 自检循环触发 | 仅在 3 份合成失败文档上启动，真实 6 份零动作，`engine=local+agent` 标记正确 |
| 导出、上传校验、任务表上限 | 均按说明生效 |
| **对齐项无法处理** | **`align` 计入待办数，却没有任何入口能消除它** → 把全部待办逐一确认完之后，`pending` 仍为 1、状态停在「进行中」，**项目永远到不了「已审完」** |

最后一项是功能性的：待办总览存在的唯一理由是回答「什么时候能收工」，
而对齐项恰好让这个答案永远无法达成。

### 已修复

- 新增对齐处理动作（提交 `kind="align"`）：`merge` 把本文件该指标改名为对方名称、两行合并；
  `keep` 只记录「确认不是同一指标」。两者都写入修订记录（`kind=align`，`name` 为两名称稳定键），
  据此从待办中移除；非法 `decision` / 空对方名称被拒。
- 前端为每条对齐项给出「统一为「X」」与「确认不合并」按钮，`holders` 指明名称落在哪份文件第几条指标。
- 新增 `tests/test_real_fixes.py`（20 项），覆盖书写精度宽容的**正反两面**、空值两态、
  对齐识别与误标防护、`merge`/`keep` 两种处理、**「审完」可达性回归**、导出列、自检补救降级。

### 复核后复现

```bash
python -m unittest discover -s tests          # 153 项通过（133 + 新增 20），10 项跳过
python scripts/demo.py --data /tmp/approval-demo
python scripts/evaluate.py --samples /tmp/approval-demo/demo-source   # 84/84、84/84、12/12，precision=recall=1.0
```

真实材料端到端：逐条确认全部待办项 + 处理指标对齐后，庆元与龙泉两个项目均到达
**「已审完」（pending=0）**；龙泉的两行 60637 平方米合并为一行，两阶段都显示该值。
HTTP 链路（上传→比对→对齐处理→导出）实测通过；前端语法检查通过。

### 仍未覆盖

真实模型质量（本机无供应商凭据）、Docker 构建（本机无守护进程）均未在本轮复核范围内。
