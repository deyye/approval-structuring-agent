# Optional override: docker compose -f compose.yaml -f compose.secrets.yaml up -d --build
services:
  app:
    environment:
      LLM_API_KEY: ''
      LLM_API_KEY_FILE: /run/secrets/llm_api_key
    secrets:
      - llm_api_key
secrets:
  llm_api_key:
    file: ./secrets/llm_api_key.txt
